interface:
  display_name: "iBrain Review"
  short_description: "Run a bounded read-only iBrain review"
  default_prompt: "Use $ibrain-review to check iBrain or run a bounded read-only repository review."
policy:
  allow_implicit_invocation: false
