interface:
  display_name: "Dev Integration"
  short_description: "Validate implemented behavior across system boundaries"
  default_prompt: "Use $dev-integration to execute the approved INT scenarios."
policy:
  allow_implicit_invocation: false
